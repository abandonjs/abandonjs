/**
 * @title equalHelper
 * @param {unknown} compareValue
 * @param {unknown} beCompareValue
 * @returns {boolean}
 */
export type EqualHelper = (
  compareValue: unknown,
  beCompareValue: unknown
) => boolean
